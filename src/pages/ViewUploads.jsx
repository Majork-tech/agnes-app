import { Typography, Container } from '@mui/material';

export default function ViewUploads() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        View Uploads
      </Typography>
      <Typography>View student uploads here</Typography>
    </Container>
  );
}