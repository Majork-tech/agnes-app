import { Typography, Container } from '@mui/material';

export default function ViewResults() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        View Results
      </Typography>
      <Typography>Quiz results will be displayed here</Typography>
    </Container>
  );
}