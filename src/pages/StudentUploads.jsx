import { Typography, Container } from '@mui/material';

export default function StudentUploads() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Student Uploads
      </Typography>
      <Typography>Manage student uploads here</Typography>
    </Container>
  );
}