import { Typography, Container } from '@mui/material';

export default function Invoices() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Invoices
      </Typography>
      <Typography>Invoice management interface here</Typography>
    </Container>
  );
}