import { Typography, Container } from '@mui/material';

export default function UploadVideos() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Upload Videos
      </Typography>
      <Typography>Video upload interface here</Typography>
    </Container>
  );
}