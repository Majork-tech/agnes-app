import { Typography, Container } from '@mui/material';

export default function RegisterStudent() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Register Student
      </Typography>
      <Typography>Student registration form here</Typography>
    </Container>
  );
}